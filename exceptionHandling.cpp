#include <iostream>
using namespace std;

// There are three keywords, try, throw and catch, whenever the code in try block has an excecption
// it throws the exception and throw block is executed, then catch block is used

